<%= form_for( [:user, @maintenance] ) do |f| %>
  <p><%= f.label :due %>:
    <%= f.date_select :due %></p>
  <p><%= f.label :complete?, "Complete" %>:
    <%= f.check_box :complete? %><br></p>
  <p><%= f.label :notes %>:<br>
    <%= f.text_area :notes %></p>
  <%= f.submit %>
<% end %>